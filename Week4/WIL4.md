# What I Learned
* 라이브러리 버전 호환 문제로 인해 오류만 잔뜩 떠서 별로 얻을 수 있는게 없었다.
* Deprecated된 기능은 언젠가는 완전히 삭제되므로 쓰지 말아야 한다는 교훈을 얻었다.

* ```pd.crosstab```: 특정 열의 변수를 기준으로 다른 열의 변수를 세는 함수인 듯 하다.
* ```value_counts``` 함수는 열의 각 값에 대한 모든 발생 횟수를 반환하는 함수이다.
* factorplot은 catplot으로 변경되었다고 한다. 근데 multiple values 오류는 여전하다.